# Award rebuild: lifted motion budget and expanded narrative IA

The restrained build (single 200–350ms curve, max two scroll moments, five Sections) executed correctly but reads as basic rather than premium, so we lift choreography to hero entrance 500–700ms stagger, Section settles 350–450ms, max four moments (hero entrance, hero-outro pin, shelf stagger, sweeping seconds), and expand IA to seven Sections in narrative order (Hero → Philosophy → About → Experience → Stack → Projects → Contact) with the Coming-soon shelf presented as engineered S.01–S.03 spec slots.

**Considered Options**: keep strict budget + five Sections and polish spacing only (rejected: fixes flatness without art direction); full cinematic rebuild with rich glows/parallax (rejected: tips into template/AI look, breaks TUI identity).

**Consequences**: status indicator moves 01/05 → 01/07; all six fan traces stay covert in exact placements; fonts stay grotesk + mono with fixed clamp scale; depth stays restrained (hero-only glow + faint noise); `prefers-reduced-motion` still kills everything.
